export enum InfoBoxPosition {
  Left = 'left',
  Right = 'right',
}

export enum LineOpenClass {
  Open = 'timeline__line--open',
  Closed = 'timeline__line--closed',
}

export enum DotVisibleClass {
  Show = 'timeline-section__dot--show',
  Hide = 'timeline-section__dot--hide',
}

export enum InfoBoxVisibleClass {
  Show = 'timeline-section__info-box--show',
  Hide = 'timeline-section__info-box--hide',
}